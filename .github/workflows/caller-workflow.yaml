name: Caller Workflow

on:
  workflow_dispatch:

#run-name: ${{ github.event.inputs.name }} 
jobs:  
  build:
    # uses: rapdev-io/jon-test-open/.github/workflows/reusable-test.yml@main
    uses: rapdev-io/jon-test-open/.github/workflows/reusable-deploy.yml@main
